# PhiGraph v0.7 Operational Intelligence

Version 0.7 adds ranked recommendations, a human-approved intervention registry,
before/after outcome evaluation, and local SQLite memory for confirmed incidents.

PhiGraph never executes real operational actions automatically.
