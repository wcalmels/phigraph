# TUCH PhiGraph Core v3.8.0

PhiGraph Code gains deterministic commit snapshots, explicit requirement trace graphs, provider-neutral model adapters, isolated patch evaluation, and benchmark confidence intervals. Remote repositories remain read-only and proposed patches are evaluated only in temporary copies.
