# TUCH PhiGraph Core v3.7.0

PhiGraph Code validation release: read-only GitHub connector, reproducible multi-model benchmark suites, extended deterministic verification, and automated JSON/Markdown scientific reports.

## Safety
- GitHub integration performs GET requests only.
- No commits, pull requests, deployments, or repository mutations.
- Verifier remains allow-listed; arbitrary shell commands are rejected.
