# ADR-009: Evidence-gated PhiGraph Code benchmark

Status: Accepted

Decision: introduce a deterministic benchmark harness that compares naive agent-report acceptance with PhiGraph evidence-gated acceptance. Only allow-listed verification commands are executable.

Consequence: PhiGraph can now quantify false-completion blocking on controlled repositories while preserving shadow-first safety.
