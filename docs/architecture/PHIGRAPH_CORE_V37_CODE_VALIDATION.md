# PhiGraph Core v3.7 — Code Validation

v3.7 converts the single-agent harness into a repeatable experiment layer. It evaluates multiple model reports against the same repository evidence, captures reported cost and latency, and generates machine-readable and human-readable reports.

The GitHub connector is intentionally read-only. Remote mutation remains outside the authority boundary.
